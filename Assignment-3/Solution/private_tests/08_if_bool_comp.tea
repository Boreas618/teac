fn foo(a:int, b:int)->int;

fn main(a:int, b:int)->int{
    let c:MyStruct;
    c.node = 3;
    c.len = 20;
    
    if(225 > c)
    {
        c.node = foo(a, b);
    }
    else
    {
        c.len = foo(b, b);
    }
}

struct MyStruct { 
    node:int,
    len:int  
}

/*
    key:
    Typecheck error in line 8, col 8: None native type are not comparable!
*/

