fn main()->int{
    let i:int = 100;
    let sum:int = 0;
    let x:int = 3000;
    let y:int = 3000;
    putint(x);
    putch(10);

    if (x > 2000) {
        while (x < 5000) {
            x = x + 1;
            sum = sum + i;
            putint(x);
            putch(10);
            while (sum < x) {
                sum = i + sum;
            }
        }
    }

    ret 0;
}