struct Node {
    l:int,
    r:int
}

let a:Node;
let b:Node;
let c[300]:Node;

fn main()->int{
    let d[500]:Node;
    let count:int;
    count = 0;

    c[0].r = 200;
    d[20].l = 1;
    a.r = 1;
    b.l = a.r;

    while(count < 10) {
        c[count].l = 1 + c[count].r;
        putint(c[count].l);
        putch(10);

        d[count].r = c[count].l  + c[count].r + count;
        count = count + 1;

    }
    count = 0;
    while(count < 10) {
        putint(d[count].r);
        putch(10);
        count = count + 1;
    }

    ret 0;
}