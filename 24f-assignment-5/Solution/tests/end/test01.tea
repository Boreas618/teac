
fn main()->int {
    let x:int=2;
    let y:int=3;
    let z:int;
    let a:int;
    let hash:int=5381;
    z=getint();
    a = x + y;

    let b:int; 
    let c:int=0;
    if (z > a) {
        b = z - a;
        let i:int=0;
        while(i<b) {
            if ((i-i/2*2) == 0) {
                c =c+x * y;
            } else {
                c =c+ y + z;
            }
            c=c-c/hash*hash;
            i=i+1;
        }
    } else {
        b = a - z;
        c = x * z;
    }
    let result:int;
    result = c + b;
    putint(result);
    putch(10);
    ret 0;
}
