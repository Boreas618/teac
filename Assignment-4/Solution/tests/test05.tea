let N:int;

let newline:int;

fn factor(n:int)->int
{
    let i:int;
    let sum:int;
    sum=0;
    i=1;
    while(i<(n+1))
    {
        if(((n/i)*i) == n)
        {
            sum=sum+i;
        }
        i=i+1;
    }
    ret sum;
}

fn main()->int
{
    N=4;
    newline=10;
    let i:int;
    let m:int;
    m = 1478;
    let t:int;
    ret factor(m);
}