#use std;

fn main()->i32
{   
    timer_start(3);
    let i;
    let sum:i32;
    let a[10]:i32;
    sum=0;
    i=0;
    while(i<10)
    {
        a[i]=i+1;
        i=i+1;
    }
    let x:i32;
    let high:i32;
    let low:i32;
    let mid:i32;
    let n:i32;
    n=10;
    x=getint();
    high=n-1;
    low=0;
    mid=(high+low)/2;
    let flag:i32;
    flag=0;
    i=0;
    let j:i32;
    j=0;
    while((i<10) && (flag==0))
    {
        if(a[i]==x)
        {
            flag=1;
            j=i;
        }
        
        i=i+1;
       
    }
    if(flag==1)
    {
        putint(j);
    }
    else
    {
        x = 0;
        putint(x);
    }

    x = 10;
    putch(x);
    timer_stop(53);
    ret 0;
}