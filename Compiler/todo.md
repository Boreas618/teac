- graph 面向对象，模板

- bg 根据 graph 变化（柏露）

- flowgraph 根据 graph 变一下（陈实立）

- ig 根据 graph 变动（瀚神）

- liveness 根据 graph 变动（戴江凡）

- temp：（陈实立）

    templabel 变成 string，

    temp，只留 num, info, type

    templist 改 tempvec

- string 用 std::string

- symbol 删掉

- checked malloc 换成 new

- 单向链表改双向链表（瀚神）



ast 后面重写（柏露）

assem 后面重写（temp 用 tempvec）（陈实立）

treep 后面重写（在 tiger ir 中增加类型）（戴江凡）

指令格式重构，增加打印函数

