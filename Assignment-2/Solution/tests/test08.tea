let gad[10]:int = {gb+26};

fn foo_null();
fn bar_null();
fn putint(num:int);
fn putch(ch:int);

fn main()->int{
    let lb:int = ((1+2) <= 3);
    let lc:int = 0;
    foo_null();
    bar_null();
    foo_null();
    ret 1+-lb+lc;
}

fn foo_null(){
    let i:int = 0;
    while((i<10)){
        putint(gad[i]);
        putch(32); // blank
    }
    putch(10); // enter
}

fn bar_null(){
    let i:int = 1;
    while((i<10)){
        gad[i] = i;
    }
    i = 1;
    while((i<10)){
        if((i == 8)){
            continue;
        }
        gad[i] = gad[i]*2;
    }
}
